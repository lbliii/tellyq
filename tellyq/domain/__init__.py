"""Provider-independent immutable contracts and deterministic playback policy.

Use ``values`` for records, ``ports`` for structural I/O contracts and ``policy``
for pure transitions. Importing this package does not initialize any adapters.
"""
