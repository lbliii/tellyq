Describe the concrete problem and resulting behavior.

Validation performed (normally `uv run --locked poe preflight`):

If playback behavior changes, distinguish requested commands, receiver observations,
and any visual confirmation. State untested hardware or service assumptions.
